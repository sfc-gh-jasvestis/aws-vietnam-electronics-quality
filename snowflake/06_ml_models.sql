-- ============================================================================
-- 06_ML_MODELS.SQL — ML Functions for Quality Analytics & Defect Prediction
-- ============================================================================
USE DATABASE ELECTRONICS_QUALITY;
USE SCHEMA ML;

-- ML.FORECAST: ELECTRONICS_QUALITY_FORECAST
CREATE OR REPLACE SNOWFLAKE.ML.FORECAST ML.ELECTRONICS_QUALITY_FORECAST(
  INPUT_DATA => SYSTEM$REFERENCE('TABLE', 'CURATED.FORECAST_INPUT'),
  SERIES_COLNAME => 'ENTITY_ID',
  TIMESTAMP_COLNAME => 'DS',
  TARGET_COLNAME => 'Y'
  
);

CREATE OR REPLACE TABLE ML.ELECTRONICS_QUALITY_FORECAST_RESULTS AS
SELECT * FROM TABLE(ML.ELECTRONICS_QUALITY_FORECAST!FORECAST(FORECASTING_PERIODS => 14));

-- ML.ANOMALY_DETECTION: ELECTRONICS_QUALITY_ANOMALY
CREATE OR REPLACE SNOWFLAKE.ML.ANOMALY_DETECTION ML.ELECTRONICS_QUALITY_ANOMALY(
  INPUT_DATA => SYSTEM$REFERENCE('TABLE', 'CURATED.TREND_ANALYTICS'),
  SERIES_COLNAME => 'ENTITY_ID',
  TIMESTAMP_COLNAME => 'DS',
  TARGET_COLNAME => 'Y',
  LABEL_COLNAME => ''
);

CREATE OR REPLACE TABLE ML.ELECTRONICS_QUALITY_ANOMALY_RESULTS AS
SELECT * FROM TABLE(ML.ELECTRONICS_QUALITY_ANOMALY!DETECT_ANOMALIES(INPUT_DATA => SYSTEM$REFERENCE('TABLE', 'CURATED.TREND_ANALYTICS'), SERIES_COLNAME => 'ENTITY_ID', TIMESTAMP_COLNAME => 'DS', TARGET_COLNAME => 'Y'));

